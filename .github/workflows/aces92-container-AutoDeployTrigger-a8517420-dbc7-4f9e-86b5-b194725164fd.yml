name: Trigger auto deployment for aces92-container

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ dev ]
    paths:
    - '**'
    - '.github/workflows/aces92-container-AutoDeployTrigger-a8517420-dbc7-4f9e-86b5-b194725164fd.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ACES92CONTAINER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ACES92CONTAINER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ACES92CONTAINER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.ACES92CONTAINER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ACES92CONTAINER_REGISTRY_PASSWORD }}
          containerAppName: aces92-container
          resourceGroup: DefaultResourceGroup-EUS
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


